# M1 驗收報告（2026-08-31）

帳號 `970785193672`，**全部資源都在 `us-east-1`**（本機 AWS CLI 預設是 ap-southeast-1，所以每個指令都要帶 `--region us-east-1`）。

## 建好的東西

| 類別 | 名稱 |
|---|---|
| DynamoDB | `subscriptions`（PK email／SK route）、`notification_history`（PK pk／SK sent_at） |
| Lambda | `flight-save-subscription`、`flight-list-subscriptions`、`flight-parser`、`flight-parser-wrapper`、`flight-fare-notification` |
| API Gateway | `flight-api` → `https://bmzjswvj8l.execute-api.us-east-1.amazonaws.com`（`POST /subscribe`、`GET /subscriptions`，CORS 全開） |
| SQS | `flight-fare-queue`（VisibilityTimeout 180s > Lambda 30s） |
| S3 | `flight-config-970785193672/flight-routes.json`（東京＋首爾兩條航線） |
| EventBridge | `flight-price-check`，`rate(30 minutes)` → `flight-parser-wrapper` |
| IAM | `flight-lambda-role`（Secrets `flight/*`、兩張表、S3 前綴、SQS 五個動作、InvokeFunction flight-parser） |
| Secrets Manager | `flight/travelpayouts`、`flight/resend`、`flight/supabase`（＋M0 的 `github/personal-access-token`） |
| 前端 | Dashboard 兩張方案卡（目標價、已訂閱徽章、更新目標價）→ Vercel `https://flight-price-notifier.vercel.app` |

## 驗收表

| 檢查 | 狀態 | 實測 |
|---|---|---|
| A1/A2 兩張表 ACTIVE | ✅ | describe-table 皆 ACTIVE |
| B1 secrets 齊全 | ✅ | travelpayouts / resend / supabase / github |
| B2/B3 IAM + save Lambda | ✅ | 角色政策五段；函式建立成功 |
| C2 訂閱列（無 subscription_status） | ✅ | `test@example.com#TPE-TYO` 欄位僅 9 個，無 status |
| D2 API → 寫入列 | ✅（curl） | `POST /subscribe` 回 200 並寫入列；**瀏覽器實測待你登入後點一次** |
| E2 已訂閱狀態 | ✅（curl） | `GET /subscriptions?email=` 回傳該列；UI 已上線待你目視 |
| F1/F2/F3 routes + queue + 部署 | ✅ | routes ETag `83eb3211…`；CLI zip 直傳（本機有 shell，不需 flight-seed 橋接） |
| G2/G3 parser 跑得動、票價合理 | ✅ | TPE-TYO 7,182 TWD（MM）／TPE-SEL 4,303 TWD（IT） |
| H1/H2 命中→入列、雙幣別 body | ✅ | `cheapest`(TWD) 必有，`cheapest_usd` 有值時附上 |
| H5 未達標不入列 | ✅ | 目標價 1,000 的測試訂閱 → `matched: 0` |
| I1 排程啟用 | ✅ | rule ENABLED、target = wrapper |
| I3 排程實際觸發 | ⏳ | 30 分鐘內首次自動觸發，之後看 CloudWatch |
| J2/J3 consumer 接上 | ✅ | event-source mapping Enabled；三個 SQS 權限齊；VisibilityTimeout 180 |
| K2 收到信 | ✅ | Resend 200，兩封已寄至 fred88wen@gmail.com |
| K3 無付款門檻 | ✅ | 程式與資料皆無 subscription_status |
| L1 去重擋掉重複 | ✅ | log：`skipped (deduped) … within 24h floor` |
| L3 大跌重發 | ✅ | log：`sent alert … (drop >= 20% vs NT$4,303)` |

## 已知限制

- 寄件人仍是 `onboarding@resend.dev`，**只會真的送達 Resend 帳號本人信箱**（fred88wen@gmail.com）。其他收件人會 403，程式判定為永久失敗直接丟棄不重試。M3 驗證網域後解除。
- `GET /subscriptions?email=` 沒有驗證身分（課程設計如此），正式環境要在 Lambda 驗 Supabase JWT。
- `index.html` 內 `TP-VERIFY-START…END` 是 Travelpayouts 網站驗證用的腳本，驗證通過後可整段刪除。
- Travelpayouts marker（分潤）未設定，訂購連結沒有帶 `?marker=`；要開啟就用 `put-secret-value` 把 token + marker 一起寫回 `flight/travelpayouts`。

## 常用指令

```bash
export PATH="$PATH:/c/Program Files/Amazon/AWSCLIV2"; export MSYS_NO_PATHCONV=1
aws logs filter-log-events --log-group-name /aws/lambda/flight-parser --region us-east-1 --query "events[].message" --output text
aws logs filter-log-events --log-group-name /aws/lambda/flight-fare-notification --region us-east-1 --query "events[].message" --output text
python aws/build_zips.py && aws lambda update-function-code --function-name flight-parser \
  --zip-file fileb://D:/OneDrive/Claude/projects/fare-finder-pro/aws/build/parser.zip --region us-east-1
```
