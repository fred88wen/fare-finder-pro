# shop.goboss.tw 視覺改版 — 拷問決策摘要（2026-09-22）

> 本檔由 `/grill-advisor` 產生：Sonnet 執行問答與查證，關鍵判斷點由 Opus 顧問把關。三個決策點顧問都攔下或修正過，細節見下方「顧問攔下的點」。

## 背景事實（動手前查證，非猜測）

- **shop.goboss.tw ≠ 電商賣場**：實為 `fare-finder-pro` 專案（原機票降價通知，2026-09-02 轉型），賣「Site Watch」網站監控訂閱（uptime + 網域到期提醒），React 19 + Vite + Tailwind v4 + Radix/shadcn，`react-router-dom`。頁面在 `src/pages/LandingPage.tsx`。
- **綠界仍是 stage 測試商店**（`aws/M2-驗收報告.md`：`merchant_id=3002607`、`env=stage`、`amount=300`）——目前真實訪客無法真的完成付款。
- **Vercel Web Analytics 從未啟用**（API 直接回 `web_analytics_not_enabled`）——「沒有人要買」目前是印象，不是數據；不知道是零流量還是有流量不轉換。
- **goboss.tw 部落格 4 篇引流文章，3 篇 CTA 仍指向 `goboss.tw/subscribe/`（電子報），未導向 shop.goboss.tw**（僅弱點掃描一篇已導向）。
- репo 無 `CLAUDE.md`；`AGENTS.md` 標示連接 Lovable，**正常 commit+push 無礙，但不可 force-push/rebase/amend 已推送的 commit**。
- getdesign.md 的 Jasper `.md` 需付費（頁面未列明確金額），未逐一翻過目錄確認有無免費同類——**這件事本身沒有查到底，不當作「沒有免費選項」的定論**，但這場決定不需要它（見決策 3）。

## 決策記錄

### 決策 1：範圍＝只做視覺改版（選項 B）

流量診斷（開 Vercel Analytics）與綠界轉正式商店，**這場都不做**，使用者已知情選擇，留給之後另外處理。

**顧問攔下的點**：這代表「視覺改版能不能讓『沒人買』好轉」這個前提，這場**不會被驗證**。改版做得再好，checkout 還是測試店、也不知道有沒有訪客。這件事必須寫進本摘要，不能因為選了乾脆的範圍就從結論裡消失。

### 決策 2：價格顯示改「月繳 99／年繳 499」，後端另外處理（選項 1）

查證 `aws/shared/ecpay_common.py`：目前只有**單一全域 `amount` 欄位**（預設 300），**沒有方案概念、沒有年繳邏輯**。「月繳99／年繳499」不是改一個數字，是新功能（雙方案選擇＋年繳扣款週期）。

使用者選擇：**①頁面先照新價顯示，後端之後另外做**。

**顧問攔下的點**：這會造成頁面寫 99/499、後端唯一設定的金額還是 300、且沒有年繳方案可以真的扣款的不一致狀態。因為綠界目前是測試店，現在不會被真實使用者發現，**但轉正式商店前必須回頭補後端**——這是待辦，不是已解決。300→99 是 67% 降幅、499/年約 5.6 個月月費，使用者未進一步說明定價策略，視為「先用這組數字，之後可能還會調」。

### 決策 3：視覺方向＝借用 Jasper 版面結構，配色維持 goboss.tw 品牌色；文章區塊保留

實際截圖 jasper.ai 真站（非下載付費 .md，僅作方向參考，不複製其圖案/品牌元素）確認風格關鍵字：editorial 雜誌感、粗襯線標題＋簡潔內文、粉彩色塊網格背景、大留白、手繪感 FAQ、大數字統計證明、真人照片。

使用者決定：
- **配色 A**：保留 goboss.tw 主色（品牌一致性優先），借用 Jasper 的**版面結構**（色塊分區、字體對比、留白節奏），不換色調
- **「網站出事了怎麼辦」4 篇文章區塊保留**，改版後放進新版面

**顧問攔下的點（結構性缺口，需在實作時明講，不可用假資料填）**：Jasper 的版面結構高度依賴「證明區塊」——大數字統計（10,000+／60%／3x）、客戶 logo 牆、真實使用者引言。**Site Watch 目前沒有真實客戶、沒有真實 uptime 統計、沒有 logo 可放**。借用 Jasper 結構時，這些槽位要嘛：
- 用 4 篇文章區塊頂替「內容深度/信任」的角色（已定案保留）
- 其餘槽位（客戶證言／logo 牆／大數字統計）**這場先不做**，不得捏造數字或虛構客戶名單頂替（違反全域規則與 frontend-design skill 的禁止假資料原則）

## 遺留風險（下次開工前必看，三項全部來自顧問攔下）

1. **前提從未被診斷**：綠界仍 stage、Analytics 從未開——視覺改版不預期能解決「沒人買」。
2. **頁面價格與後端收費不一致**：99/499（頁面）vs 300（後端唯一設定，無年繳）。轉正式商店前必須回頭補齊。
3. **CTA 仍只有「Sign in / 登入」**（沿用 09-16 舊決定，這場未變動）——陌生訪客沒有非登入的入口。
4. **驗證工具已知限制**：`ui-verify` 的 `verify_ui.py` 對用 `IntersectionObserver` 捲動觸發淡入的區塊會拍成空白（未模擬捲動），已用 `--computed`/`--count` 交叉驗證確認內容其實正常渲染，只是截圖抓不到。**新版面不要沿用捲動觸發淡入這個 pattern**（改預設可見，避免同樣的驗證死角，也避免真實使用者在不捲動的裝置/爬蟲上看到空白）。改版後的前後對比截圖需另外處理（模擬捲動或移除該動畫再拍）。

## 追加：導購文案範圍（2026-09-22 同日追加，動手前必看）

使用者在決策 3 定案後，追問「會不會逐一討論文案」，確認結果：**文案本來就在範圍內**——使用者最早的需求就寫「缺乏設計感、導購內容也不足」，決策 1 的 B 排除的只有流量診斷與綠界轉正式商店，沒有排除文案。使用者具體想要的內容：

1. **「5 個為什麼要買」**（5 Why）——自家產品賣點/立場表達，可以放手寫，沒有真實性問題
2. **「網域忘記續約造成嚴重後果」的案例**——這項有真實性分級限制，必須遵守：
   - **真實公開事件**（有報導可查證的品牌網域到期出包）——可指名道姓，最有說服力
   - **情境式假設**，明講「如果⋯會怎樣」——合法但不可寫成像真有這麼一家客戶
   - **假客戶/假數據**——不可以用，跟決策 3 裡 Jasper 證明區塊留白同一條規則（全域禁止捏造規則 + frontend-design skill 的 no-fake-data 限制）

**寫這個案例時的已知衝突，動手前務必避開**：專案 memory（`project_fare_finder_pro.md`）記載 `.tw`/`.com.tw` 網域到期查詢目前是**系統性失敗**（IANA 無備援伺服器，`ccrdap.twnic.tw` 對任何 client 一律 426），這條限制尚未解決。**案例不可選 `.tw` 網域**，否則等於拿產品現在查不到的那個 TLD 當恐懼訴求主打，會打自己臉。案例應選 `.com`/`.net` 類，或情境模糊化不指定 TLD。

**文案本身要套用 `writing` 專案 CLAUDE.md 的寫作禁止清單**（即使這個檔案在 `fare-finder-pro`）：不用行銷黑話（賦能/痛點/閉環等）、不用「隨著⋯的時代」這類廢話開場、要有具體數字而非「很多」、結尾不說教。

**流程待確認（下次開工第一件事）**：文案要①**跟著版面設計一起討論**（提一個區塊的版面結構＋草稿文案，使用者當場反應調整，一塊一塊過）、還是②**先把全部文案寫成大綱核准，核准完才動手排版**？我建議①（Jasper 色塊分區結構會限制每塊能放多少字，文案先寫完常要整段重寫遷就版面），但使用者尚未回覆選哪個，**新 session 開工第一步要先問這題**，不要自己預設走①。

## 實作筆記（下次開工照做）

- **價格文案已知出現在**：`src/pages/DashboardPage.tsx`、`src/components/MonitorCard.tsx`、`src/lib/flight-api.ts`（另外 3 個「300」命中是 shadcn UI 元件內部樣式數值，如 `command.tsx`/`navigation-menu.tsx`/`sheet.tsx`，與定價無關，不要誤改）。
- **`index.html` 的靜態 `<title>`/`<meta>` 是第二個來源**，跟 `useDocumentMeta()` 動態注入分開——09-02 轉型時就漏改過一次（`f679364` 才補上）。這次若改標語/加價格到 meta，兩處都要改。
- **git 操作**：正常 `commit` + `push` 到 Lovable 連接的分支沒問題；**禁止 force-push / rebase / amend 已推送的 commit**（`AGENTS.md` 明載）。
- 設計工具：不需要（也買不到免費版）getdesign.md 的 Jasper `.md`，改用 `web-design` skill（載入 `frontend-design` + `taste-skill` + `impeccable`），以 jasper.ai 實際截圖當方向參考自行設計，避免複製其品牌圖案（它是真實商業品牌）。

## 執行最佳建議

| 項目 | 建議 |
|---|---|
| **Model** | 這場拷問（診斷/決策）用 Sonnet + advisor 顧問已完成；**下一階段實際動手設計/改 code** 屬「寫程式碼新功能/架構決策」，依全域 CLAUDE.md 對照表建議切回 **Opus**，Effort **Medium**（視覺設計 + 多檔案改動，非連動全錯的高風險步驟） |
| **是否開新 session** | 建議開新 session——本場已完整定案三個決策，下一階段是實作，context 沒必要延續拷問過程 |
| **繼續關鍵字** | 在 `d:/OneDrive/Claude/projects/fare-finder-pro` 下，讀 `SHOP-REDESIGN-DECISION-2026-09-22.md` 全文（含「追加：導購文案範圍」一節）確認三項決策＋文案範圍與真實性分級限制＋遺留風險，**第一步先問使用者「文案要跟版面一起討論、還是先出大綱核准」**（未定案，不要預設走建議選項），載入 `web-design` skill，以 jasper.ai 截圖（scratchpad 裡已有，可重用或重截）為版面結構參考、goboss.tw 現有色票為配色基準，對 `src/pages/LandingPage.tsx` 逐區塊設計＋草擬文案（含 5 Why、網域到期案例——避開 `.tw`、套用 writing 專案禁止清單），保留 4 篇文章區塊、CTA 維持登入、加入月繳99/年繳499價格顯示、避免捲動觸發淡入動畫，完成後跑 `npx tsc --noEmit` + `npm run build`，本機 `npm run preview` 用 ui-verify 截圖桌機+手機驗證，再詢問使用者是否 commit + push 部署 |
